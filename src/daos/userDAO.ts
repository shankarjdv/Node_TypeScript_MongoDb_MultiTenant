// userDao.ts

// import { getDB } from '../dbConnections';

const collectionName = 'users';

export const createUser = async (userData: any, dbName: string) => {
  // const db = getDB(dbName);
  // const result = await db.collection(collectionName).insertOne(userData);
  return {}//result;//.ops[0];
};

// ...
